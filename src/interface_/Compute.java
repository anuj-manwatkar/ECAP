package interface_;

public interface Compute {
	void add(); //inside the interface the method is always public & abstract (write abstract in front of method or not doesn't put any error)
	abstract void sub();

}
